using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class knife : MonoBehaviour
{

    private void OnTriggerEnter2D(Collider2D other)
    {

        if (other.CompareTag("fly_enemies")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("enemies1")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("enemies2")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("enemies3")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("enemies4")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("enemies5")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("enemies6")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("enemies7")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("enemies8")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("enemies9")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("enemies10")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("enemies11")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("enemies12")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("enemies13")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("enemies14")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("enemies15")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("enemies16")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("enemies17")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("enemies18")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }


        else if (other.CompareTag("bull1")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("bull2")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("bull3")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("bull4")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("bull5")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("bull6")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("bull7")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("bull8")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("bull9")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("bull10")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("bull11")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("bull12")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
        else if (other.CompareTag("bull13")) // Eğer karaktere çarparsa
        {
            Destroy(gameObject); // Düşmanı yok et 
        }
    }
}
